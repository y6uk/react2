import React from "react";
import s from './Music.module.css';

const Music = (props) => {
    return (
        <div>
            <span className={s.pree}>Music</span>
        </div>
    )
};

export default Music;