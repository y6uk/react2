import React from "react";
import s from './Settings.module.css';

const Settings = (props) => {
    return (
        <div>
            <span className={s.pree}>Settings</span>
        </div>
    )
};

export default Settings;