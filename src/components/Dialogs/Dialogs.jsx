import React from "react";
import s from './Dialogs.module.css';

const Dialogs = (props) => {
    return (
        <div>
            <span className={s.pree}>Dialogs</span>
        </div>
    )
};

export default Dialogs;